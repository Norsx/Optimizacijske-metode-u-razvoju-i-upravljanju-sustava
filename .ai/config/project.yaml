name: "Optimizacijske metode u razvoju i upravljanju sustava"
type: "seminar"              # seminar | thesis | paper | homework
latex_format: "fsb-seminar"  # fsb-seminar | fsb-thesis | fsb-paper
brain_mode: "global"         # none | global
# RAG is always available via AgentBrain (~/.agentbrain). Embeddings use Gemini
# if GEMINI_API_KEY is set in .env, otherwise local sentence-transformers.

# --- Metadata za naslovnicu (latex_architect popunjava main.tex) ---
author_name: "Ivan Noršić"   # Ime i prezime studenta
course_name: "OPTIMIZACIJSKE METODE U RAZVOJU I UPRAVLJANJU SUSTAVA"
seminar_title: "Kvadratično programiranje, robusna optimizacija i robusna stabilizacija dinamičkih sustava"
seminar_title_short: "QP, robusna optimizacija i stabilizacija"
professor_title: "Prof. dr. sc."
professor_name: "Andrej Jokić"

# --- Build & page limit ---
max_pages: 30                # build skripta upozorava ako PDF premašuje N stranica
                             # (12 je bio default predloška; rad ima 4 zadatka sa slikama)
dist_version: "dev"          # build piše PDF u dist/<dist_version>/ (npr. "v1.0" za predaju)

# --- Opcijski popisi (latex_architect uključuje ako je true) ---
include_lof: false           # \listoffigures
include_lot: false           # \listoftables

# AgentBrain version used during bootstrap
agentbrain_version: "2.2.0"
agentbrain_commit: "8983e7c"
